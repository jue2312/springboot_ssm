server:
  port: 80
spring:
  datasource:
    username: root
    password: 3388047303
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://dsy:3306/ssm
  banner:
    location: 阿弥陀佛.txt
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tb_ #table 前缀
      id-type: auto #配置自增规则
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #配置日志输出都控制台
  mapper-locations: classpath:mapper/*.xml